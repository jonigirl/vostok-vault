# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Environment files
.env
.env.*

# Local dev data (gitignored — personal save scans, item reference DB)
data/

# One-off dev exploration scripts with hardcoded local paths
scripts/scan_saves.py

# Promo assets and tooling — local only, never pushed
scripts/make_promo_images.py
